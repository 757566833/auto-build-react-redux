const reducer = (state, action) => {
    switch (action.type) {
        case 'Component':
            switch (action.func) {
                
                default:
                    return state;
            }
        case 'IndexView':
            switch (action.func) {
                
                default:
                    return state;
            }
        
        default:
            return state;
    }
};
export default reducer;

