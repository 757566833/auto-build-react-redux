// import { SurpriseViewRef } from '../../view/SurpriseView/SurpriseView_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                SurpriseViewRef.setfunc(data.data);
    //            } else {
    //                SurpriseViewRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            SurpriseViewRef.networkError(result);
    //        }
    // }
}