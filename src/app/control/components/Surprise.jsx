// import { SurpriseRef } from '../../components/Surprise/Surprise_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                SurpriseRef.setfunc(data.data);
    //            } else {
    //                SurpriseRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            SurpriseRef.networkError(result);
    //        }
    // }
}