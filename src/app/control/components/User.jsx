// import { UserRef } from '../../components/User/User_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //           let data = eval(`(${result})`);
    //           if (data.code == 0) {
    //                UserRef.setfunc(data.data);
    //            } else {
    //                UserRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            UserRef.networkError(result);
    //        }
    // }
}