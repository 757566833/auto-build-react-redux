const username = 'username'
const usernameState = {
    'name': username,
    'type': 'string',
    'default': 'user'
}
const setUsername = {
    'method': 'setUsername',
    'parameter': username
}

module.exports = { usernameState, setUsername };